/*
Go is not an OO Language like C++ / Java etc.
It does not has objects nor inheritances and hence no polymorphism and overloading.

Go, on the other hand have "structures", which can be asociated with "Methods" and "Composition".
Definition of a "Structure" and "Class"are verymuch similar.
*/

type Voter struct {
  Name string
  Age int
}

/* Now, we need to add "Method" to this structure, just like you'dhave methods as a part of "class".

